# Studio import checklist — 7-island revamp

The one manual step the overnight build could not do for you: re-importing the
regenerated `.glb`s into Studio and re-exporting `assets/Assets.rbxm`. Until
this is done the game RUNS but shows fallbacks: new islands are "coming soon"
in the travel menu, ranged weapons and the boat use their procedural shapes,
new creatures use recolored stand-ins. Nothing errors — it is all the
established missing-mesh behavior.

Follow assets/README.md's import steps (glTF import, keep collision fidelity,
replace under ReplicatedStorage/Assets keeping the EXACT names, then
right-click Assets → Save to File… → assets/Assets.rbxm, restart rojo serve,
commit the .rbxm).

## Imports needed (one Studio session, in any order)

| # | File | Replaces / adds under Assets | Notes |
|---|---|---|---|
| 1 | `assets/island_pack.glb` | `IslandPack` (or per-island models) | **OWED AGAIN as of 2026-08-27 (7165cf4)**: the swamp was RESTARTED — the pack's Swamp group is now step 3 of the rebuild: the colored landform + the marsh (`Swamp_Base` + `Swamp_Water`, 9 sunken pools; still no trees/props/dock/foam — delete every old Swamp child when replacing). collision fidelity now matters on ONE swamp part only: PreciseConvexDecomposition on `Swamp_Base` (the pool beds are real carved bowls; a Default hull caps them). Every other swamp mesh - trees included - is pass-through at runtime; solid trunks come from invisible runtime colliders (Shared/Data/SwampTrees.luau), so no import setting can create phantom walls. **The VOLCANO was RESTARTED too (2026-08-27, this pack regen)**: its group is now step 1 of its rebuild — ONE bare `Volcano_Base` landform (a clean ~205-stud cone + crater dish on a broad ash apron; no lava/trail/props/dock/foam — delete every old Volcano child when replacing; PreciseConvexDecomposition on `Volcano_Base` AND `Volcano_DeadTrees` - a box hull over a spread dead-tree crown is an invisible wall). Steps 2+3 followed same-day: `Volcano_Lava` (fishable, Neon) + the 46 dead giants are in the pack now - the "no lava" note above is superseded. Ice/gloom/wreck are byte-identical to the previous pack (SEED leak pinned at 7 in the volcano entry — mesh bottoms verified -9.00/-16.53/-18.25). As of dd1ec0a the pack bundles ALL SEVEN islands: Island, Volcano, **Swamp, Frostmaw, Gloomtrench, Wreckwater, Maelstrom** (the Maelstrom group is identical to row 1b's standalone glb — importing the pack covers it; see 1b for its collision notes). One import covers every island. WorldService clones each child group by model name. Use PreciseConvexDecomposition collision fidelity on the `<Name>_Base` landforms and solid props (players walk them); water/foam slabs can stay default. **Frostmaw's walkable ice sheet MUST be PreciseConvexDecomposition too**: with the Default hull the pre-cut fishing holes get capped by an invisible floor (the exact volcano-crater gotcha from context.md) — you'd walk over the holes and every cast at them would hit the cap. **NPC HOUSES (2026-08-29): every `*_Hut_Walls` and `*_Hut_Roof` part MUST be PreciseConvexDecomposition.** The six houses (Island_Hut_*, Swamp_Hut_*, Frostmaw_Hut_*, Volcano_Hut_*, Gloomtrench_Hut_*, Wreckwater_Hut_*) are walk-in: with a Box or Hull fidelity the shell becomes a solid block, the doorway fills with an invisible wall and the interior is unreachable - the same failure as the volcano crater cap. Collision fidelity cannot be set from code, so this is import-time or nothing. Their `_Props`/`_Glow`/`_Canvas`/`_Trim` layers are non-collidable at runtime and need no special fidelity. |
| 1b | `assets/island_maelstrom.glb` | `Maelstrom` (new) | **NEW 2026-08-27**: the Maelstrom's authored mesh (drowned storm-shoal, fight stacks, titan fangs, the doomed fleet, chains, storm-fire) — the finale stops being nine grey boxes. Import as `ReplicatedStorage/Assets/Maelstrom` (9 objects under one container, keep exact names). **PreciseConvexDecomposition on `Maelstrom_Base` and `Maelstrom_Platforms`** — the platforms are the Kraken fight's floor and travel's landing probe. Until imported the procedural stand-in keeps running; the whirlpool disc is procedural either way. (Covered by row 1's pack as of dd1ec0a — import EITHER this or the pack, whichever lands last wins; the collision notes here apply to the pack's Maelstrom group too.) |
| 2 | `assets/weapon.glb` | `WeaponPack` | **OWED as of c562dd6 (icon-matched melee round; supersedes 0c80a76)**: all 20 melee models rebuilt to rod-pack quality, then re-matched to the user's canon icon set (guns untouched). Swing/reload animation fixes are code-side and live on boot; the remodeled melee needs this import. NOTE: the Studio Assets.rbxm re-export sitting in the tree predates c562dd6 - re-import weapon.glb even if you just did an import pass. Import once, using the LAST weapon.glb commit in git log. |
| 3 | `assets/boat.glb` | `BoatPack` (new) | Six tier hulls + Helm seat markers + trophy-shelf sterns (variant names in boat_gen.py). On first import, sanity-check the bow orientation in Studio (authored bow = Blender -y, same convention gotcha as the rod pack). |
| 4 | `assets/creatures.glb` | `CreaturePack` | **OWED AGAIN as of 2026-08-27 (ba93efb)**: the boss-redesign pass rebuilt Gnashroot, Rimefang, Pyrelisk, Noctyss, AdmiralWrack, Kraken AND KrakenTentacle bespoke — until re-import the fights run with the OLD boss meshes (movesets are code-side and already live). Import the latest creatures.glb, replace `CreaturePack`, re-export Assets.rbxm. |
| 5 | `assets/fish.glb` | `FishPack` | Regenerated with 18 new species (34 total), including the four Legendary chase fish. |
| 6 | `assets/rod.glb` | `RodPack` | Regenerated with all 36 rod variants (cut rods pruned; every island's five added, the Maelstrom five included). Replace the old RodPack, keep the exact name. |
| 7b | `assets/arena_brinejaw.glb` | `BrinejawArena` (new) | **NEW 2026-08-29 (boss revamp)**: the cove boss's lair arena - the Tidebreak Spire (sand ring, broken lighthouse, reef-rock boundary, three ReefStone slam-bait targets). Import STANDALONE as `ReplicatedStorage/Assets/BrinejawArena` (13 objects, keep exact names - BossArenaService finds the group by `BrinejawArena_Base` and swaps it in for the procedural disc automatically). **PreciseConvexDecomposition on `BrinejawArena_Base`, `_Spire`, `_Rocks`, and the `_ReefStone*` parts** (players walk/fight on all of them); foam + coral are stripped of collision at runtime. The serpent meshes are a later import - the arena works standalone. |
| 7c | `assets/boss_brinejaw.glb` | `BrinejawPack` (new) | **NEW 2026-08-29 (boss revamp)**: the cove serpent's PIECES - `Brinejaw_Head/_HeadBone/_Eyes/_Frill/_Jaw/_JawBone/_Seg/_SegFin/_Rattle` (9 objects). Import as `ReplicatedStorage/Assets/BrinejawPack`, exact names. **Collision fidelity does not matter** - the client clones these into ~60 massless, collision-free parts and drives them along a path; the fight's hitboxes are server math plus the head/rattle parts. Not needed until the Brinejaw fight logic lands; the arena (row 7b) stands alone before that. |
| 7j | `assets/arena_rimefang.glb` | `RimefangArena` (new) | **NEW 2026-08-29 (frost boss lane)**: Rimefang's lair - the Floe. A raft of sea ice with a pressure ridge around its rim, two grounded bergs, a whaler frozen into it and a whale carcass (9 objects, keep exact names). Import STANDALONE as `ReplicatedStorage/Assets/RimefangArena` - BossArenaService finds the group by `RimefangArena_Base` and swaps it in for the procedural disc automatically, no code change. **PreciseConvexDecomposition on `RimefangArena_Base`, `_Ridge`, `_Bergs`, `_Drifts` and `_Hull`** - players walk the floe, shelter behind the bergs and the wreck, and climb the drift ramps onto them; a box hull over a ramp is an invisible wall. `_Foam` is stripped of collision at runtime. The boss meshes are a later import - the arena stands alone before that. |
| 7k | `assets/boss_rimefang.glb` | `RimefangPack` (new) | **NEW 2026-08-29 (frost boss lane)**: Rimefang's PIECES - `Rimefang_Head/_Jaw/_Teeth/_Eyes/_Rime/_Body/_Belly/_Fin/_Pec/_Fluke/_Harpoon` (11 objects). Import as `ReplicatedStorage/Assets/RimefangPack`, exact names. **Collision fidelity does not matter** - the client clones these into ~40 massless, collision-free parts and drives them along a path; the fight's hitboxes are server math plus the head. Not needed until the Rimefang fight logic lands; the arena (row 7d) stands alone before that. |
| 7g | `assets/arena_wrack.glb` | `WrackArena` (new) | **NEW 2026-08-29 (boss revamp)**: Admiral Wrack's lair arena - the Careenage (a wet ribbed tidal shoal walkable to r 78, ringed by a palisade of fifteen half-sunk hulls at r 80-88, a collapsed careening cradle at the head of the shoal, grounding furrows ploughed down the haul lane, and six breakwater hulks standing in the open sand as the fight's cover). Import STANDALONE as `ReplicatedStorage/Assets/WrackArena` (22 objects, 16 `M_Wrack_*` materials, keep exact names - BossArenaService finds the group by `WrackArena_Base` and swaps it in for the procedural disc automatically). **PreciseConvexDecomposition on `WrackArena_Base`, `_Fleet`, `_FleetRibs`, `_FleetMasts`, `_Cradle`, and the six `_Hulk1..6`** - those are the only things players stand on, walk into, or hide behind, and the six hulks above all: they are load-bearing cover from Wrack's broadsides, so a Default hull over them changes the fight. Every object whose name carries `Deco` is stripped of collision AND query at runtime, so its import setting does not matter (the Rootmere rule); `_Foam` is stripped too and its name also earns the tide-riding behaviour. The Wrack boss mesh and the fight logic are separate later passes - the arena works standalone. |
| 7f | `assets/boss_gnashroot.glb` | `GnashrootPack` (new) | **NEW 2026-08-30 (swamp boss redesign)**: the fen's MUD COLOSSUS - a hunched mass of wet mud with eyes across its flanks, bog stone in its back, and a root-knot core. BODY, all authored in one space so the client places the cluster with a single CFrame: `Gnashroot_Mass/_Legs/_Head/_Jaw/_Maw/_Fangs/_Eyes/_Core/_Stones/_Drips`. LIMBS, driven as two ChainPose chains: `_Arm/_ArmKnot/_Hand`. 13 objects, import as `ReplicatedStorage/Assets/GnashrootPack`, exact names. **SET `Gnashroot_Core` TO NEON AT IMPORT TIME.** This is not optional and nothing will do it for you: the `Glow`-in-the-name -> Neon rule is honoured by BossArenaService for ARENA meshes only, and boss packs go through their `<Boss>BodyController`, which has no such rule. The core is the fight's punish target - the one thing the player is taught to shoot - and without Neon it is a flat green lump. `Gnashroot_Eyes` is deliberately DARK and glossy (not Neon); the core is the only light on the creature. Collision fidelity does not matter otherwise - the client clones these into massless, collision-free parts. Supersedes the earlier half-gator pack of the same name. |
| 7d | `assets/arena_gnashroot.glb` | `GnashrootArena` (new) | **NEW 2026-08-29 (boss revamp)**: the swamp boss's lair arena - the Rootmere (a wide black mere ringed by a BARE walkable peat bank, four stump platforms, a drowned cypress grove boundary). Import STANDALONE as `ReplicatedStorage/Assets/GnashrootArena` (16 objects, keep exact names - BossArenaService finds the group by `GnashrootArena_Base` and swaps it in for the procedural disc automatically). **PreciseConvexDecomposition on `GnashrootArena_Base`, `_Grove`, and the four `_Stump*` parts** - those are the only things players stand on or walk into. Every object whose name carries `Deco` is stripped of collision AND query at runtime, so its import setting does not matter (this is the general form of the Brinejaw foam/coral rule). The Gnashroot boss meshes are a later import - the arena works standalone. |
| 7h | `assets/arena_noctyss.glb` | `NoctyssArena` (new) | **NEW 2026-08-29 (boss revamp)**: the gloom boss's lair arena - the Choirfloor (an abyssal basalt shelf torn open by a central pit, seven chitin SOCKETS the Lantern Choir grows out of, six knife-edge shadow fins, and a boundary that is a drop into black rather than a wall). Import STANDALONE as `ReplicatedStorage/Assets/NoctyssArena` (21 objects, keep exact names - BossArenaService finds the group by `NoctyssArena_Base` and swaps it in for the procedural disc automatically). **PreciseConvexDecomposition on `NoctyssArena_Base`** is REQUIRED, not optional: the pit is a carved bowl and a default hull CAPS it (the volcano-crater gotcha) - you would walk on air over the hole the boss rises through. Same setting on `_Rim`, `_Slab` (the ramp out of the pit), `_Fins`, and the seven `_Socket*` parts; a default hull is fine for `_Teeth`, `_Stacks`, `_Elders`, `_Bones`, `_PitWater`. `_Deco*`, `_GlowCoral`, `_GlowKelp` and `_Foam` are stripped of collision at runtime, so their import setting does not matter. The Noctyss/choir meshes are a later import - the arena works standalone. |
| 7i | `assets/boss_noctyss.glb` | `NoctyssPack` (new) | **NEW 2026-08-29 (boss revamp)**: the gloom boss's PIECES, two assemblies in one pack (14 objects, exact names). The CHOIR STALK is a chain - `Noctyss_StalkSeg/_StalkFin/_StalkHood/_StalkCage/_StalkBulb/_StalkBarbs/_StalkRoot` - instanced ~13 parts per stalk up a ChainPose curve, seven stalks on the arena's socket ring. The MAW is placed - `Noctyss_Skull/_SkullBone/_Jaw/_JawBone/_Gullet/_Eyes/_Barbels` - two CFrames, the jaw's being the skull's turned about the authored hinge. Import as `ReplicatedStorage/Assets/NoctyssPack`. **Collision fidelity does not matter** - every piece is cloned into massless, collision-free parts; the fight's hitboxes are server math, and the walkable floor inside her jaw is anchored parts the fight spawns, NOT this mesh. **`Noctyss_StalkBulb` and `Noctyss_Gullet` want Neon** (they are the only light in the arena; the client parents a PointLight to each bulb and swaps Neon off when a lure is doused or a stalk dies). Not needed until the fight logic lands; the arena (row 7e) stands alone before that. |
| 7m | `assets/boss_kraken.glb` | `KrakenPack` (new) | **NEW 2026-08-29 (the octopus rebuild)**: the final boss's PIECES - the HEAD assembly `Kraken_Head/_Crown/_Crust/_Eyes/_Pupil/_Beak` (six objects authored in ONE space, so the client places them all with a single CFrame) plus the three ARM pieces `Kraken_ArmSeg/_ArmTip/_Sucker`, 9 objects. Import as `ReplicatedStorage/Assets/KrakenPack`, exact names. **Collision fidelity does not matter** - KrakenBodyController clones these into ~170 massless, collision-free parts (the head, then one ChainPose arm per live tentacle); the fight's hitboxes are server math plus the hidden head model and the eight `kraken_tentacle` creatures. `Kraken_Eyes` and `Kraken_Sucker` want **Neon** (the row's markColor - storm-lit eyes and suckers); `_Pupil` must NOT be Neon or the slit stops reading. **DO NOT scale the pieces on import**: the arm segments are authored 5.4 long to be planted every 2.6, and that overlap is the only reason an arm reads as one limb instead of a string of beads. Until imported the fight draws in fallback cylinders - correctly shaped and posed, just untextured, so nothing is ever invisible. |
| 7 | `assets/armor.glb` | `ArmorPack` (new) | **NEW 2026-08-28 (armor system)**: the wearable armor sets - `<SetPrefix>_Helm/_Chest/_Legs` per set (Chitin + Boneplate in the skeleton; more sets land behind it). Import as `ReplicatedStorage/Assets/ArmorPack`, keep the exact object names. No collision concerns: ArmorService strips physics from clones at equip time. Until imported, worn armor shows as simple tinted shell boxes (deliberate fallback, nothing invisible). |

## After importing

1. Restart `rojo serve` and reconnect.
2. Playtest checkpoints (from docs/revamp-plan.md's Verification):
   - Travel menu shows all islands; swamp opens at L8 after Brinejaw.
   - Cast into Swamp_Water pools → swamp roster rolls; same per island
     (Frostmaw_IceHoles / Gloomtrench_DarkWater / Wreckwater_Bay).
   - Ranged weapons draw their meshes in hand; boat spawns with its hull tier.
   - Each boss summons from its island's Call bait and drops its heart.
3. Commit the updated `assets/Assets.rbxm`.

This file was written by the overnight coordination session; each art commit
that needs an import is also flagged in its own commit message.

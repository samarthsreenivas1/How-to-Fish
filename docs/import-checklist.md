# Studio import checklist — 7-island revamp

The one manual step the overnight build could not do for you: re-importing the
regenerated `.glb`s into Studio and re-exporting `assets/Assets.rbxm`. Until
this is done the game RUNS but shows fallbacks: new islands are "coming soon"
in the travel menu, ranged weapons and the boat use their procedural shapes,
new creatures use recolored stand-ins. Nothing errors — it is all the
established missing-mesh behavior.

Follow assets/README.md's import steps (glTF import, keep collision fidelity,
replace under ReplicatedStorage/Assets keeping the EXACT names, then
right-click Assets → Save to File… → assets/Assets.rbxm, restart rojo serve,
commit the .rbxm).

## Boss packs: the glowing parts, and which are your job

Read this once; it applies to every `<Boss>Pack` row below. Studio's glTF import
lands every MeshPart on flat `Plastic`, so a part that is meant to glow has to
have its Material set to `Neon` by somebody.

For ARENAS that somebody is the game. `BossArenaService.placeAuthored` sets
`Enum.Material.Neon` on every part of an arena whose OBJECT NAME contains `Glow`
(the contract is spelled out in its comment block at ~line 84). That is why no
arena row ever asks you to touch a material.

BOSS PACKS get no such rule - they are cloned by their `<Boss>BodyController`,
which copies the part exactly as imported, so `Glow` in the name buys nothing on
this side. Several controllers now assert their own fight-critical glow just
after the clone (Pyrelisk's `_Core`/`_Seam`/`_Eyes`, Gnashroot's `_Core`,
Brinejaw's and Rimefang's `_Eyes`, and Noctyss's stalk bulb, which the fight
lights and douses), and for those the import setting is harmless but not needed.
Wrack's controller goes one better and runs the `Glow` name rule itself, so
every emissive part of `WrackPack` is covered too. The ones that genuinely
depend on you, as of 2026-08-30: `KrakenPack`'s `Kraken_Eyes`/`_Sucker` and
`NoctyssPack`'s `Noctyss_Gullet`.

At the keyboard the rule is simply: set what a row asks for and nothing it does
not - a redundant Neon costs nothing, while the parts a row says must NOT be Neon
(`Kraken_Pupil`, `Gnashroot_Eyes`) are dark on purpose. (When a glow is something
the fight depends on, the lasting fix is a line in the controller, the way
Pyrelisk does it, rather than an import step someone has to remember.)

## Imports needed (one Studio session, in any order)

| # | File | Replaces / adds under Assets | Notes |
|---|---|---|---|
| 1 | `assets/island_pack.glb` | `IslandPack` (or per-island models) | **OWED AGAIN as of 2026-08-27 (7165cf4)**: the swamp was RESTARTED — the pack's Swamp group is now step 3 of the rebuild: the colored landform + the marsh (`Swamp_Base` + `Swamp_Water`, 9 sunken pools; still no trees/props/dock/foam — delete every old Swamp child when replacing). collision fidelity now matters on ONE swamp part only: PreciseConvexDecomposition on `Swamp_Base` (the pool beds are real carved bowls; a Default hull caps them). Every other swamp mesh - trees included - is pass-through at runtime; solid trunks come from invisible runtime colliders (Shared/Data/SwampTrees.luau), so no import setting can create phantom walls. **The VOLCANO was RESTARTED too (2026-08-27, this pack regen)**: its group is now step 1 of its rebuild — ONE bare `Volcano_Base` landform (a clean ~205-stud cone + crater dish on a broad ash apron; no lava/trail/props/dock/foam — delete every old Volcano child when replacing; PreciseConvexDecomposition on `Volcano_Base` AND `Volcano_DeadTrees` - a box hull over a spread dead-tree crown is an invisible wall). Steps 2+3 followed same-day: `Volcano_Lava` (fishable, Neon) + the 46 dead giants are in the pack now - the "no lava" note above is superseded. Ice/gloom/wreck are byte-identical to the previous pack (SEED leak pinned at 7 in the volcano entry — mesh bottoms verified -9.00/-16.53/-18.25). As of dd1ec0a the pack bundles ALL SEVEN islands: Island, Volcano, **Swamp, Frostmaw, Gloomtrench, Wreckwater, Maelstrom** (the Maelstrom group is identical to row 1b's standalone glb — importing the pack covers it; see 1b for its collision notes). One import covers every island. WorldService clones each child group by model name. Use PreciseConvexDecomposition collision fidelity on the `<Name>_Base` landforms and solid props (players walk them); water/foam slabs can stay default. **Frostmaw's walkable ice sheet MUST be PreciseConvexDecomposition too**: with the Default hull the pre-cut fishing holes get capped by an invisible floor (the exact volcano-crater gotcha from context.md) — you'd walk over the holes and every cast at them would hit the cap. **NPC HOUSES (2026-08-29): every `*_Hut_Walls` and `*_Hut_Roof` part MUST be PreciseConvexDecomposition.** The six houses (Island_Hut_*, Swamp_Hut_*, Frostmaw_Hut_*, Volcano_Hut_*, Gloomtrench_Hut_*, Wreckwater_Hut_*) are walk-in: with a Box or Hull fidelity the shell becomes a solid block, the doorway fills with an invisible wall and the interior is unreachable - the same failure as the volcano crater cap. Collision fidelity cannot be set from code, so this is import-time or nothing. Their `_Props`/`_Glow`/`_Canvas`/`_Trim` layers are non-collidable at runtime and need no special fidelity. |
| 1b | `assets/island_maelstrom.glb` | `Maelstrom` (new) | **NEW 2026-08-27**: the Maelstrom's authored mesh (drowned storm-shoal, fight stacks, titan fangs, the doomed fleet, chains, storm-fire) — the finale stops being nine grey boxes. Import as `ReplicatedStorage/Assets/Maelstrom` (9 objects under one container, keep exact names). **PreciseConvexDecomposition on `Maelstrom_Base` and `Maelstrom_Platforms`** — the platforms are the Kraken fight's floor and travel's landing probe. Until imported the procedural stand-in keeps running; the whirlpool disc is procedural either way. (Covered by row 1's pack as of dd1ec0a — import EITHER this or the pack, whichever lands last wins; the collision notes here apply to the pack's Maelstrom group too.) |
| 1c | `assets/island_pack.glb` (same file as row 1) | nine islet groups under `IslandPack` | **FULLY REDESIGNED 2026-09-02** (supersedes every earlier note in this row): the nine islets no longer share one landform - a heeled basalt wedge split by a cleft (`Bellbuoy`), a drowned church you walk on (`Chapel`), two stacks joined by a sea-arch (`Rookery`), a four-platform floating hamlet (`Ferryraft`), a broken caldera round a hot lagoon (`Boilshoal`), a cliff-edged shelf over the trench (`Lampwork`), a wading flat of anchors (`Anchorage`), a 200-stud crescent spit (`Whalefall`), and a cathedral of struck needles (`Loadstone`). Row 1's single import still covers all nine; exact names; every bbox bottom measures -9.00 so none declares a meshBottom. **PreciseConvexDecomposition is load-bearing on ~28 parts** - a hull seals a doorway, fills the arch, or flattens a stair on every one of these: `Bellbuoy_Wedge` (the cleft is walked through) `_Dinghy` `_Gantry` `_Base`; `Chapel_Roof` (the HOLED walkable deck - you fish through the tears) `_Stone` (open belfry + porch) `_Yard` (individual flags); `Rookery_Stacks` (the arch has a real hole - a hull fills the channel) `_Ledges` `_Eyrie` (stilts + open deck); `Ferryraft_Cabin` `_Bridges` `_Bow` `_Barrels` `_Barge` `_Gear`; `Boilshoal_Rim` (two channels a hull would seal - the lagoon becomes unreachable) `_Terraces` `_Hut`; `Lampwork_Shop` (walk-in shop + lean-to + jetty piles + lighthouse stair) `_Base` (the plumb cliff and the void below it); `Anchorage_Shack` (the gangway) `_Iron` (17 concave anchors - a hull is one box over the whole garden); `Whalefall_Bar` (a hull fills the lagoon) `_Bones` (walk-through ribcage, walk-in rib-house); `Loadstone_Rubble` (ramps + pad) `_Shack` `_Spires` (a concave cluster you walk between). **No manual material step**: all 220 islet parts take colour from `MESH_COLOR` (derived from this very glb - `tools/check_islet_colors.py` reads the pack and fails on drift in either direction), nine Neon parts come from `MESH_MATERIAL`, and 45 non-collide rulings apply at runtime. **`Boilshoal_Lagoon` is a fishable water surface** (FISHABLE_NAMES + WATERS_BY_PART, drawing the ocean pool) - it must stay CanQuery-able, which the runtime handles; nothing to do at import. **Do not scale on import.** |
| 2 | `assets/weapon.glb` | `WeaponPack` | **OWED as of c562dd6 (icon-matched melee round; supersedes 0c80a76)**: all 20 melee models rebuilt to rod-pack quality, then re-matched to the user's canon icon set (guns untouched). Swing/reload animation fixes are code-side and live on boot; the remodeled melee needs this import. NOTE: the Studio Assets.rbxm re-export sitting in the tree predates c562dd6 - re-import weapon.glb even if you just did an import pass. Import once, using the LAST weapon.glb commit in git log. |
| 3 | `assets/boat.glb` | `BoatPack` (new) | Six tier hulls + Helm seat markers + trophy-shelf sterns (variant names in boat_gen.py). On first import, sanity-check the bow orientation in Studio (authored bow = Blender -y, same convention gotcha as the rod pack). |
| 4 | `assets/creatures.glb` | `CreaturePack` | **OWED AGAIN as of 2026-08-27 (ba93efb)**: the boss-redesign pass rebuilt Gnashroot, Rimefang, Pyrelisk, Noctyss, AdmiralWrack, Kraken AND KrakenTentacle bespoke — until re-import the fights run with the OLD boss meshes (movesets are code-side and already live). Import the latest creatures.glb, replace `CreaturePack`, re-export Assets.rbxm. |
| 5 | `assets/fish.glb` | `FishPack` | Regenerated with 18 new species (34 total), including the four Legendary chase fish. |
| 6 | `assets/rod.glb` | `RodPack` | Regenerated with all 36 rod variants (cut rods pruned; every island's five added, the Maelstrom five included). Replace the old RodPack, keep the exact name. |
| 7b | `assets/arena_brinejaw.glb` | `BrinejawArena` (new) | **NEW 2026-08-29 (boss revamp)**: the cove boss's lair arena - the Tidebreak Spire (sand ring, broken lighthouse, reef-rock boundary, three ReefStone slam-bait targets). Import STANDALONE as `ReplicatedStorage/Assets/BrinejawArena` (13 objects, keep exact names - BossArenaService finds the group by `BrinejawArena_Base` and swaps it in for the procedural disc automatically). **PreciseConvexDecomposition on `BrinejawArena_Base`, `_Spire`, `_Rocks`, and the `_ReefStone*` parts** (players walk/fight on all of them); foam + coral are stripped of collision at runtime. The serpent meshes are a later import - the arena works standalone. |
| 7c | `assets/boss_brinejaw.glb` | `BrinejawPack` (new) | **NEW 2026-08-29 (boss revamp)**: the cove serpent's PIECES - `Brinejaw_Head/_HeadBone/_Eyes/_Frill/_Jaw/_JawBone/_Seg/_SegFin/_Rattle` (9 objects). Import as `ReplicatedStorage/Assets/BrinejawPack`, exact names. **Collision fidelity does not matter** - the client clones these into ~60 massless, collision-free parts and drives them along a path; the fight's hitboxes are server math plus the head/rattle parts. Not needed until the Brinejaw fight logic lands; the arena (row 7b) stands alone before that. |
| 7j | `assets/arena_rimefang.glb` | `RimefangArena` (new) | **NEW 2026-08-29 (frost boss lane)**: Rimefang's lair - the Floe. A raft of sea ice with a pressure ridge around its rim, two grounded bergs, a whaler frozen into it and a whale carcass (9 objects, keep exact names). Import STANDALONE as `ReplicatedStorage/Assets/RimefangArena` - BossArenaService finds the group by `RimefangArena_Base` and swaps it in for the procedural disc automatically, no code change. **PreciseConvexDecomposition on `RimefangArena_Base`, `_Ridge`, `_Bergs`, `_Drifts` and `_Hull`** - players walk the floe, shelter behind the bergs and the wreck, and climb the drift ramps onto them; a box hull over a ramp is an invisible wall. `_Foam` is stripped of collision at runtime. The boss meshes are a later import - the arena stands alone before that. |
| 7k | `assets/boss_rimefang.glb` | `RimefangPack` (new) | **NEW 2026-08-29 (frost boss lane)**: Rimefang's PIECES - `Rimefang_Head/_Jaw/_TeethUpper/_TeethLower/_Eyes/_Rime/_Body/_Belly/_Fin/_Pec/_Fluke` (11 objects). Import as `ReplicatedStorage/Assets/RimefangPack`, exact names. **Materials are set in CODE, so there is NO manual material step**: RimefangBodyController states Neon on `_Eyes` and Ice on `_Fin` when it clones them. A glTF import lands every part SmoothPlastic, and unlike arena meshes a boss pack gets no name-driven material pass, so this is stated in the rig rather than left to be remembered. **Collision fidelity does not matter** - the client clones these into ~40 massless, collision-free parts and drives them along a path; the fight's hitboxes are server math plus the head. Not needed until the Rimefang fight logic lands; the arena (row 7d) stands alone before that. |
| 7g | `assets/arena_wrack.glb` | `WrackArena` (new) | **REDESIGNED 2026-09-01 (user brief: "much bigger without any clutter... make the floor walkable and plain")**: Admiral Wrack's lair arena - the Careenage. **A BROAD PLAIN BEACH, WALKABLE TO r 132** (was 78; 2.9x the floor area), shelving gently from 0.45 studs at the water's edge to 4.85 at the head of the beach - a shape a ship could be hauled onto, which is what a careenage is. **Nothing stands on it.** The six breakwater hulks, the careening cradle, the grounding furrows, the spoil heaps, tide puddles, barrels and chain runs are all DELETED: they were justified as cover from the broadsides and were never cover, because ProjectileService.tick does no raycast and nothing in this game occludes a bullet. The boundary is a palisade of THIRTY half-sunk hulls at r 141-160, open 46 degrees at bearing 90 (the mouth - lowest ground, the tide-wrack line, and the way in). Import STANDALONE as `ReplicatedStorage/Assets/WrackArena` (**10 objects**, down from 22; 9 `M_Wrack_*` materials, keep exact names - BossArenaService finds the group by `WrackArena_Base` and swaps it in for the procedural disc automatically). **`BossArenas.wreck.meshBottom` must be -13.5**, MEASURED off the build (the base's underside cone now closes below the whole fleet on purpose, so the landform is the bbox floor again rather than whichever hull happens to hang lowest); leaving it at the old -9.5 lifts the arena 4 studs. **PreciseConvexDecomposition on `WrackArena_Base`, `_Fleet`, `_FleetRibs`, `_FleetMasts`** - the floor and the palisade are the only things players stand on or walk into, and there is no cover to preserve any more. Every object whose name carries `Deco` is stripped of collision AND query at runtime, so its import setting does not matter (the Rootmere rule); `_Foam` is stripped too and its name also earns the tide-riding behaviour. `_DecoGhostGlow` (renamed from `_DecoGhostFire`/`_DecoGhostLamps`, now one object) carries `Glow`, so it goes Neon by BossArenaService's own marker rather than by the grandfathered names. Previews: `arena_wrack_preview.png`, `_plan`, `_ground`, and `_staged`/`_staged_plan` (the boss on it, with 5-stud stand-ins at r 60/95/128). |
| 7p | `assets/boss_wrack.glb` | `WrackPack` (new) | **NEW 2026-08-30 (d63a849, wreck boss lane); REDESIGNED 2026-09-08 from direct player feedback**: Admiral Wrack himself - his flagship now standing UPRIGHT and keel-bedded in the shoal, with the drowned admiral on her quarterdeck. The 48-degree careen is GONE, the ship is a little over half its old bulk, and the four floating gun "batteries" are now four ACTUAL CANNONS built into the hull. **This one is ONE COHERENT MODEL, not a pieces pack** - every other boss pack in this file is a chain rig whose parts get instanced along a curve, and this is not: there is no path module and nothing WrackBodyController has to pose, all 25 objects are authored in one shared space at their final positions, and the whole ship goes down with a SINGLE CFrame (`Wrack_Base` is the anchor; its centre is the boss origin at sand level, authored z=0 being the shoal). ~48 studs bow to stern, 26.5 to the masthead and 34.4 to the sabre tip (was 87 and 72 - the old hull could not be framed from the drop ring without pitching the fight camera to within 4 degrees of its ceiling), 18 materials all prefixed `M_Wrack_Adm` - deliberately disjoint from the 16 `M_Wrack_*` names row 7g's Careenage owns, because Blender reuses material datablocks by name and a collision would have the two silently repaint each other. Import as `ReplicatedStorage/Assets/WrackPack`, exact names: that is the `<Boss>Pack` convention every other pack follows (each controller declares `local PACK_NAME = "<Boss>Pack"`), and **this import is what makes the authored ship appear** - WrackBodyController draws him from this pack. Until imported the fight draws stand-ins - blocks at every piece's authored size, colour and place, so nothing is ever invisible; importing is a visual upgrade, not a prerequisite. **The four cannons are the fight's targets and the code addresses them BY NAME - keep `Wrack_CannonBow` (+X, the chase gun run out over the beakhead), `Wrack_CannonStarboard` (+Y, the broadside gun forward of the waist), `Wrack_CannonStern` (-X, the chase gun through the transom under his own boots) and `Wrack_CannonPort` (-Y, the broadside gun abaft the waist) exactly as they are. The old `Wrack_BatteryDeck/Keel/Stern/Top` names NO LONGER EXIST in the mesh.** Silence all four and the hull opens for the melee window, and that window is real geometry: `Wrack_ShellUpper` (the PORT plate) and `Wrack_ShellLower` (the STARBOARD plate) swing off the keel axis (+50 and -50 degrees about boss-local X through Roblox-space (0, 1.1, 0)), peeling outboard and down like double doors and leaving a 12-stud hole amidships with `Wrack_HeartGlow` burning in the hold between them. `Wrack_MuzzleGlow` is a small flare MODULE - authored at the origin with +X as the bore, instanced once per muzzle - decoration only, and nothing breaks if it is never placed. **No manual material step for his glows**: WrackBodyController asserts the `Glow`-in-the-object-name rule itself, as a substring match after the clone, so `Wrack_HeartGlow` and `Wrack_EyeGlow` come out Neon whatever the importer gave them - and `Wrack_MuzzleGlow` gets it from the same function the day something places it. **Collision fidelity does not matter** - none of him is walked on: he is a planted gun platform you fight from the Careenage's sand (walkable to r 132, and bare - the rebuild took the breakwater hulks out, so there is no cover on it and every answer in the fight is a walk), there is no climb route like Pyrelisk's `_WalkArm`/`_WalkDeck`, and the hitboxes are server math (`hitRadius`) plus the four cannon creatures. **DO NOT scale on import**: he is authored at final size, and `Wrack_Base` sits at z=0 because that IS the shoal's surface, so boss space and arena space share a height and the placement needs no offset. Previews in `assets/`: `boss_wrack_preview.png`, `_staged`, `_staged_fight`, `_staged_plan` (the high, pitched-down fight camera), `_staged_opened` (the hull swung open), `_staged_scale` (stand-ins at r 60 / 88 / 128, the ranges the fight is played at), and the per-cannon sheet `_staged_gun_bow/_starboard/_stern/_port` - one shot per cannon, since the cannons are what the player shoots. |
| 7f | `assets/boss_gnashroot.glb` | `GnashrootPack` (new) | **NEW 2026-08-30 (swamp boss redesign)**: the fen's MUD COLOSSUS - a hunched mass of wet mud with eyes across its flanks, bog stone in its back, and a root-knot core. BODY, all authored in one space so the client places the cluster with a single CFrame: `Gnashroot_Mass/_Legs/_Head/_Jaw/_Maw/_Teeth/_Fangs/_Eyes/_Core/_Stones/_Drips` (`_Teeth` is the PALATE row, split from `_Fangs` 2026-09-01 so the upper teeth stop rotating with the jaw). LIMBS, driven as two ChainPose chains: `_Arm/_ArmKnot/_Hand`. **14 objects**, import as `ReplicatedStorage/Assets/GnashrootPack`, exact names. Neon on `Gnashroot_Core` is BELT-AND-BRACES here, not load-bearing: GnashrootBodyController sets it after the clone whichever branch built the piece, because it is a fight rule wearing a material rather than art. Set it anyway if it is free, but do not chase it. `Gnashroot_Eyes` is deliberately DARK and glossy (not Neon) - the core is the only light on the creature, and a second neon piece makes the punish target ambiguous. Collision fidelity does not matter otherwise - the client clones these into massless, collision-free parts. **DO NOT scale on import** - the rig multiplies the imported BaseSize by the row's 1.5 itself, so a scaled import double-scales the colossus. Supersedes the earlier half-gator pack of the same name. |
| 7d | `assets/arena_gnashroot.glb` | `GnashrootArena` (new) | **NEW 2026-08-29 (boss revamp)**: the swamp boss's lair arena - the Rootmere (a wide black mere ringed by a BARE walkable peat bank, four stump platforms, a drowned cypress grove boundary). Import STANDALONE as `ReplicatedStorage/Assets/GnashrootArena` (16 objects, keep exact names - BossArenaService finds the group by `GnashrootArena_Base` and swaps it in for the procedural disc automatically). **PreciseConvexDecomposition on `GnashrootArena_Base`, `_Grove`, and the four `_Stump*` parts** - those are the only things players stand on or walk into. Every object whose name carries `Deco` is stripped of collision AND query at runtime, so its import setting does not matter (this is the general form of the Brinejaw foam/coral rule). The Gnashroot boss meshes are a later import - the arena works standalone. |
| 7h | `assets/arena_noctyss.glb` | `NoctyssArena` (new) | **NEW 2026-08-29 (boss revamp)**: the gloom boss's lair arena - the Choirfloor (an abyssal basalt shelf torn open by a central pit, seven chitin SOCKETS the Lantern Choir grows out of, six knife-edge shadow fins, and a boundary that is a drop into black rather than a wall). Import STANDALONE as `ReplicatedStorage/Assets/NoctyssArena` (21 objects, keep exact names - BossArenaService finds the group by `NoctyssArena_Base` and swaps it in for the procedural disc automatically). **PreciseConvexDecomposition on `NoctyssArena_Base`** is REQUIRED, not optional: the pit is a carved bowl and a default hull CAPS it (the volcano-crater gotcha) - you would walk on air over the hole the boss rises through. Same setting on `_Rim`, `_Slab` (the ramp out of the pit), `_Fins`, and the seven `_Socket*` parts; a default hull is fine for `_Teeth`, `_Stacks`, `_Elders`, `_Bones`. `_Deco*` (which now includes `_DecoPitWater` - the pit's water MUST be non-solid or it caps the pit), `_GlowCoral`, `_GlowKelp` and `_Foam` are stripped of collision at runtime, so their import setting does not matter. The Noctyss/choir meshes are a later import - the arena works standalone. |
| 7i | `assets/boss_noctyss.glb` | `NoctyssPack` (new) | **NEW 2026-08-29 (boss revamp)**: the gloom boss's PIECES, two assemblies in one pack (14 objects, exact names). The CHOIR STALK is a chain - `Noctyss_StalkSeg/_StalkFin/_StalkHood/_StalkCage/_StalkBulb/_StalkBarbs/_StalkRoot` - instanced ~13 parts per stalk up a ChainPose curve, seven stalks on the arena's socket ring. The MAW is placed - `Noctyss_Skull/_SkullBone/_Jaw/_JawBone/_Gullet/_Eyes/_Barbels` - two CFrames, the jaw's being the skull's turned about the authored hinge. Import as `ReplicatedStorage/Assets/NoctyssPack`. **Collision fidelity does not matter** - every piece is cloned into massless, collision-free parts; the fight's hitboxes are server math, and the walkable floor inside her jaw is anchored parts the fight spawns, NOT this mesh. **No manual material step**: `Noctyss_StalkBulb` and `Noctyss_Gullet` are the only light in the arena, and `NoctyssBodyController.setLantern` asserts Neon (and swaps it back to plastic when a lure is doused or a stalk dies) on whichever part it is handed - imported piece or fallback block. Nothing to remember here, and nothing to re-remember if a future re-export drops materials. Not needed until the fight logic lands; the arena (row 7h) stands alone before that. |
| 7m | `assets/boss_kraken.glb` | `KrakenPack` (new) | **NEW 2026-08-29 (the octopus rebuild)**: the final boss's PIECES - the HEAD assembly `Kraken_Head/_Crown/_Crust/_Eyes/_Pupil/_Beak` (six objects authored in ONE space, so the client places them all with a single CFrame) plus the three ARM pieces `Kraken_ArmSeg/_ArmTip/_Sucker`, 9 objects. Import as `ReplicatedStorage/Assets/KrakenPack`, exact names. **Collision fidelity does not matter** - KrakenBodyController clones these into ~170 massless, collision-free parts (the head, then one ChainPose arm per live tentacle); the fight's hitboxes are server math plus the hidden head model and the eight `kraken_tentacle` creatures. **Materials need no import step**: KrakenBodyController asserts Neon on `Kraken_Eyes` and `Kraken_Sucker` in code (and deliberately NOT on `_Pupil`, which must stay matte or the slit stops reading), so a re-export that drops materials cannot break them. **DO NOT scale the pieces on import**: the arm segments are authored 5.4 long to be planted every 2.6, and that overlap is the only reason an arm reads as one limb instead of a string of beads. Until imported the fight draws in fallback cylinders - correctly shaped and posed, just untextured, so nothing is ever invisible. |
| 7 | `assets/armor.glb` | `ArmorPack` (new) | **NEW 2026-08-28 (armor system)**: the wearable armor sets - `<SetPrefix>_Helm/_Chest/_Legs` per set (Chitin + Boneplate in the skeleton; more sets land behind it). Import as `ReplicatedStorage/Assets/ArmorPack`, keep the exact object names. No collision concerns: ArmorService strips physics from clones at equip time. Until imported, worn armor shows as simple tinted shell boxes (deliberate fallback, nothing invisible). |
| 7n | `assets/arena_pyrelisk.glb` | `PyreliskArena` (new) | **NEW 2026-08-29 (volcano boss lane)**: Pyrelisk's lair - the Ashfall Throne, a volcano SUMMIT. A dead-flat rim path (r 120-232) between the caldera's lava lake and an overhanging crater lip broken by five vista notches, with the mountain's flank falling 300 studs into a cloud deck that hides the cut, the sea, and the lower mountain (12 objects, keep exact names). Import STANDALONE as `ReplicatedStorage/Assets/PyreliskArena` - BossArenaService finds the group by `PyreliskArena_Base`. **PreciseConvexDecomposition on `PyreliskArena_Rim` is REQUIRED, not optional**: its face LEANS BACK over the path and that overhang is the only thing containing the player (a humanoid walks up almost any slope) - a default hull fills the undercut and turns the containment into a ramp. Same setting on `_Flank`, `_Teeth`, `_Gate`; a default hull is fine for the flat `_Base`. Every `Deco` name (`_SeamDeco`, `_CrustDeco`, `_AshDeco`, `_BoneDeco`, `_VentDeco`, `_CloudDeco`) is stripped of collision AND query at runtime. **`_Lava` wants Neon and CanCollide off** (the lake is flush with the path and the hazard is a radius check, not a raycast), and **`_SeamDeco` wants Neon** - the four rings are the fight's distance dial. **DO NOT scale on import**: `_CloudDeco` is authored at ~1980 studs across, deliberately just inside the 2048 MeshPart cap. Two code items ride with this arena, both noted in the generator's HANDOFF lines: `BossArenaService.ringPositions` still drops challengers at `WATER_Y + 3.5` and must probe the rim path instead, and the arena needs FOG (~1800 studs) or the ocean reappears past the cloud deck's edge. |
| 7o | `assets/boss_pyrelisk.glb` | `PyreliskPack` (new) | **NEW 2026-08-29 (volcano boss lane)**: the Ashfall Colossus's PIECES - the BODY (`Pyrelisk_Core/_Torso/_Head/_Jaw/_Crown/_Eyes/_WalkDeck`), the LIMB chain (`_Shoulder/_UpperArm/_Forearm/_Hand/_WalkArm`, authored along +X from each joint so the rig walks shoulder → elbow → wrist), and two MODULES placed many times (`_Seam`, the shootable weak point and the maw glow; `_Vent`, the breakable you smash while mounted) - 14 objects, exact names. Import as `ReplicatedStorage/Assets/PyreliskPack`. **Collision fidelity matters here, unlike every other boss pack**: `_WalkArm` and `_WalkDeck` are the CLIMB ROUTE and the only collidable parts (box fidelity is exact and correct for them); every other piece is shell and is cloned into massless, collision-free parts. **`_Core`, `_Seam` and `_Eyes` want Neon** - the core shows through the gaps between shell plates, and that glow is the whole design (undamaged it is a black mountain; each broken seam is a permanent scar). **DO NOT scale on import**: the torso's base sits at authored z=0 because that IS the caldera lake's surface, so boss space and arena space share a height and the rig needs no offset. Not needed until the colossus fight logic lands; the arena (row 7n) stands alone before that. |

## After importing

1. Restart `rojo serve` and reconnect.
2. Playtest checkpoints (from docs/revamp-plan.md's Verification):
   - Travel menu shows all islands; swamp opens at L8 after Brinejaw.
   - Cast into Swamp_Water pools → swamp roster rolls; same per island
     (Frostmaw_IceHoles / Gloomtrench_DarkWater / Wreckwater_Bay).
   - Ranged weapons draw their meshes in hand; boat spawns with its hull tier.
   - Each boss summons from its island's Call bait and drops its heart.
3. Commit the updated `assets/Assets.rbxm`.

This file was written by the overnight coordination session; each art commit
that needs an import is also flagged in its own commit message.
